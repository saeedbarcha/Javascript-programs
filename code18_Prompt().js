   //*********   prompt function
     
    // showing prompt to the user
    // we cane only pass two parameters in prompt function
    var a = prompt ("Enter value:")
    // showing the value of the prompt in document
    document.write("<br>the input on the screen : "+a);
    // setting default input in prompt function
    var b = prompt("the default value:" ,"saeed"); //by writting comas we can add a default value in prompt function
    document.write("<br>the default value  in side the prompt function: "+b)
