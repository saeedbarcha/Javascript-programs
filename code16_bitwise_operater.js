//     bitwise AND operater returns   1 if both bits are haveing value 1  
    var  a =5;   // 101
    var b=6;     // 110
    var c = a&b; // 100
    document.write("<br>bitwise AND operater in java script (5&6) :"+c);
    
//     bitwise OR operater returns   1 if any of the bit if true bits are haveing value 1  
    var  a1 =5;   // 101
    var b1=6;     // 110
    var c1 = a|b; // 111
    document.write("<br>bitwise OR operater in java script (5|6) :"+c1);
  
  //     bitwise XOR operater returns   1 if  bits are not same 
    var   a2 =5;     // 101
    var b2 =6;     // 110
    var c2 = a^b;  // 011
    document.write("<br>bitwise XOR operater in java script (5^6) :"+c2);
  // returning NaN
    var num=123;
    var name="saeed";
  // this will return NaN means not a number
    document.write(num/name);
