    // typeof operater
    // is used to find the data type of an operand
//**** variable containing  number value
    var num=100;
    document.write(typeof num);
    document.write(typeof (num)); //putting circle brackets around variables is not compulsory


//****  variable containing srting value
    var name="saeed barcha";
    document.write(typeof name);
    document.write(typeof (name)); //putting circle brackets around variables is not compulsory

//****  variable containing boolean value
    var mybool=true;
    document.write(typeof mybool);
    document.write(typeof (mybool)); //putting circle brackets around variables is not compulsory

//****  variable containing object value
    var myobj={
        name:'saeed',
        age:50
    };
    document.write(typeof myobj);
    document.write(typeof (myobj)); //putting circle brackets around variables is not compulsory

//****  variable containing array value
    var myArray=[1,"saeed",333,true];
    document.write(typeof myArray);
    document.write(typeof (myobj)); //putting circle brackets around variables is not compulsory

